<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ content.title }}</title>
    <link rel="stylesheet" href="{{ asset('css/IonutsStyles.css') }}">
</head>
<body>
    <div class="container">
        <h1>{{ content.title }}</h1>
        <p>{{ content.content }}</p>
    </div>
</body>
</html>